/**
 * Client-side behaviour for Special:InfothequeCore's direct-visit
 * "create a new page" flow. No interactive behaviour needed at the
 * moment — this module currently only carries ext.infothequeCore.special.css
 * (the .ithc-preview-* styles) for the preview step's markup.
 */
